version: "3"
services:
  server:
    image: caddy:alpine
    container_name: "server_mjindra.eu"
    restart: always
    ports:
      - "80:80"
    volumes:
      - $PWD/Caddyfile:/etc/caddy/Caddyfile:ro
      - $PWD/public/:/usr/share/caddy/:ro
